

/--
  # Syntax of oracles used in cryptographic definitions

  An oracle takes in inputs, keeps a private state, perform probabilistic computations, and returns outputs. In this sense, it's the same as parties in a protocol.

  Define one party's black-box access to another party / oracle.
-/
